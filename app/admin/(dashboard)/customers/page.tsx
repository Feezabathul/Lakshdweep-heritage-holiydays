"use client";

import { useEffect, useMemo, useState } from "react";
import { CalendarDays, ChevronRight, Mail, Phone, Search, UserRound, X } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
import AdminBadge from "@/components/admin/AdminBadge";

type Booking = Record<string, unknown> & { id: string; status?: string | null };
type Customer = { key: string; name: string; email: string; phone: string; bookings: Booking[]; latestDate: string; latestStatus: string };
const valueFrom = (booking: Booking, keys: string[]) => { for (const key of keys) { const value = booking[key]; if (value !== undefined && value !== null && String(value).trim()) return String(value); } return "-"; };
const dateValue = (booking: Booking) => valueFrom(booking, ["travel_date", "start_date", "check_in", "date"]);
const formatDate = (value: string) => { if (value === "-") return value; const date = new Date(value); return Number.isNaN(date.getTime()) ? value : new Intl.DateTimeFormat("en-IN", { day: "2-digit", month: "short", year: "numeric" }).format(date); };
const statusTone = (status: string): "teal" | "amber" | "slate" | "rose" => status.toLowerCase() === "confirmed" ? "teal" : status.toLowerCase() === "cancelled" ? "rose" : status.toLowerCase() === "pending" ? "amber" : "slate";
const identity = (booking: Booking) => { const email = valueFrom(booking, ["email", "customer_email", "guest_email"]); const phone = valueFrom(booking, ["phone", "phone_number", "mobile", "customer_phone"]); const name = valueFrom(booking, ["customer_name", "guest_name", "name", "full_name", "customer"]); return { email, phone, name, key: email !== "-" ? email.toLowerCase() : `${name.toLowerCase()}|${phone}` }; };

export default function AdminCustomersPage() {
  const [bookings, setBookings] = useState<Booking[]>([]);
  const [isLoading, setIsLoading] = useState(true);
  const [errorMessage, setErrorMessage] = useState("");
  const [search, setSearch] = useState("");
  const [selected, setSelected] = useState<Customer | null>(null);

  useEffect(() => { const load = async () => { const { data, error } = await createClient().from("bookings").select("*").order("created_at", { ascending: false }); if (error) setErrorMessage(error.message.includes("relation") ? "The bookings table is not available in Supabase yet." : error.message); else setBookings((data ?? []) as Booking[]); setIsLoading(false); }; void load(); }, []);

  const customers = useMemo(() => { const grouped = new Map<string, Customer>(); for (const booking of bookings) { const person = identity(booking); const current = grouped.get(person.key); const bookingDate = dateValue(booking); if (current) { current.bookings.push(booking); if (new Date(bookingDate).getTime() > new Date(current.latestDate).getTime()) { current.latestDate = bookingDate; current.latestStatus = valueFrom(booking, ["status", "booking_status"]) || "Pending"; } } else grouped.set(person.key, { key: person.key, name: person.name, email: person.email, phone: person.phone, bookings: [booking], latestDate: bookingDate, latestStatus: valueFrom(booking, ["status", "booking_status"]) || "Pending" }); } return [...grouped.values()].sort((a, b) => new Date(b.latestDate).getTime() - new Date(a.latestDate).getTime()); }, [bookings]);
  const filtered = customers.filter((customer) => { const query = search.trim().toLowerCase(); return !query || [customer.name, customer.email, customer.phone].some((value) => value.toLowerCase().includes(query)); });

  return <div className="mx-auto max-w-[1400px] space-y-7"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-700">Relationships</p><h2 className="mt-2 text-2xl font-bold tracking-tight text-slate-950 sm:text-3xl">Customers</h2><p className="mt-2 max-w-2xl text-sm text-slate-500">Customers are grouped from your Supabase booking and enquiry records.</p></div>{errorMessage && <div role="alert" className="rounded-lg border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700">{errorMessage}</div>}<div className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><label className="relative block"><span className="sr-only">Search customers</span><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search name, email, or phone..." className="w-full rounded-lg border border-slate-200 bg-slate-50 py-2.5 pl-9 pr-3 text-sm outline-none focus:border-teal-500 focus:ring-4 focus:ring-teal-500/10" /></label></div><div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200"><thead className="bg-slate-50"><tr>{["Customer", "Email", "Phone", "Bookings", "Latest travel date", "Latest status", "History"].map((heading) => <th key={heading} className="whitespace-nowrap px-5 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{!isLoading && filtered.map((customer) => <tr key={customer.key} className="cursor-pointer hover:bg-slate-50" onClick={() => setSelected(customer)}><td className="whitespace-nowrap px-5 py-4"><span className="inline-flex items-center gap-2 text-sm font-semibold text-slate-900"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-sky-100 text-sky-700"><UserRound className="h-4 w-4" /></span>{customer.name}</span></td><td className="whitespace-nowrap px-5 py-4 text-sm text-slate-600">{customer.email}</td><td className="whitespace-nowrap px-5 py-4 text-sm text-slate-600">{customer.phone}</td><td className="whitespace-nowrap px-5 py-4 text-sm font-semibold text-slate-800">{customer.bookings.length}</td><td className="whitespace-nowrap px-5 py-4 text-sm text-slate-600">{formatDate(customer.latestDate)}</td><td className="whitespace-nowrap px-5 py-4"><AdminBadge tone={statusTone(customer.latestStatus)}>{customer.latestStatus}</AdminBadge></td><td className="whitespace-nowrap px-5 py-4"><button aria-label={`View booking history for ${customer.name}`} className="inline-flex items-center gap-1 text-sm font-semibold text-teal-700">View <ChevronRight className="h-4 w-4" /></button></td></tr>)}</tbody></table></div>{isLoading ? <div className="p-12 text-center text-sm text-slate-500">Loading customers...</div> : filtered.length === 0 ? <div className="border-t border-slate-200 px-6 py-14 text-center"><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-slate-100 text-slate-500"><UserRound className="h-5 w-5" /></div><h3 className="mt-4 text-sm font-semibold text-slate-900">{customers.length ? "No matching customers" : "No customers yet"}</h3><p className="mx-auto mt-1 max-w-sm text-sm text-slate-500">{customers.length ? "Try a different search." : "Customers will appear here when booking data is available in Supabase."}</p></div> : null}</div>{selected && <CustomerDrawer customer={selected} onClose={() => setSelected(null)} />}</div>;
}

function CustomerDrawer({ customer, onClose }: { customer: Customer; onClose: () => void }) { return <div className="fixed inset-0 z-50"><button aria-label="Close customer history" onClick={onClose} className="absolute inset-0 bg-slate-950/30" /><aside role="dialog" aria-modal="true" className="absolute right-0 top-0 h-full w-full max-w-xl overflow-y-auto bg-white shadow-2xl"><div className="flex items-start justify-between border-b border-slate-200 px-6 py-5"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-700">Customer history</p><h2 className="mt-2 text-xl font-bold text-slate-950">{customer.name}</h2><p className="mt-1 text-sm text-slate-500">{customer.email}</p></div><button onClick={onClose} aria-label="Close" className="rounded-lg p-2 text-slate-400 hover:bg-slate-100"><X className="h-5 w-5" /></button></div><div className="grid grid-cols-2 gap-3 p-6"><Info icon={Mail} label="Email" value={customer.email} /><Info icon={Phone} label="Phone" value={customer.phone} /></div><div className="px-6 pb-8"><h3 className="text-sm font-bold text-slate-900">Booking history</h3><div className="mt-3 space-y-3">{customer.bookings.map((booking) => <div key={booking.id} className="rounded-xl border border-slate-200 p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-slate-900">{valueFrom(booking, ["package_name", "package", "package_title", "tour_package"])}</p><p className="mt-1 text-xs text-slate-500">{formatDate(dateValue(booking))} · {valueFrom(booking, ["travelers", "traveller_count", "guests", "number_of_travelers"])} travelers</p></div><AdminBadge tone={statusTone(valueFrom(booking, ["status", "booking_status"]) || "Pending")}>{valueFrom(booking, ["status", "booking_status"]) || "Pending"}</AdminBadge></div></div>)}</div></div></aside></div>; }
function Info({ icon: Icon, label, value }: { icon: typeof Mail; label: string; value: string }) { return <div className="rounded-lg border border-slate-100 p-3"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-wider text-slate-400"><Icon className="h-3.5 w-3.5" />{label}</div><p className="mt-2 break-words text-sm font-medium text-slate-700">{value}</p></div>; }
